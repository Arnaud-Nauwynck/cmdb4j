

- "@fx-eval": "#phase0:for"
  end: 3 
  # #{Topology_NumberOfVirtualHost}
  template:
    - id: virtualHost#{index}
      type: ProvisionedHost

    - id: virtualHost#{index}/tomcat1
      type: Tomcat
      tomcatBase: /home/user/tomcat1

    - id: virtualHost#{index}/tomcat2
      type: Tomcat
      tomcatBase: /home/user/tomcat2
  